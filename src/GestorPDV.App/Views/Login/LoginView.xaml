<Window x:Class="GestorPDV.App.Views.Login.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="GestorPDV - Login"
        Width="420" Height="320"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{StaticResource PincelFundoClaro}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="GestorPDV" FontSize="22" FontWeight="Bold"
                   Foreground="{StaticResource PincelBarraEscura}" Margin="0,0,0,16" />

        <TextBlock Grid.Row="1" Text="Operador:" Margin="0,0,0,4" />
        <TextBox Grid.Row="1" Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,24,0,0" Padding="6" />

        <TextBlock Grid.Row="2" Text="Senha:" Margin="0,50,0,4" />
        <PasswordBox x:Name="CaixaSenha" Margin="0,74,0,0" Padding="6"
                     PasswordChanged="CaixaSenha_PasswordChanged" />

        <TextBlock Grid.Row="3" Text="{Binding MensagemErro}" Foreground="{StaticResource PincelPerigo}"
                   TextWrapping="Wrap" Margin="0,16,0,0" />

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Confirma (F12)" Style="{StaticResource BotaoPrimario}"
                    Command="{Binding ConfirmarCommand}" Width="140" />
        </StackPanel>
    </Grid>
</Window>
