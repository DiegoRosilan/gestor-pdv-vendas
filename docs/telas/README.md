# Telas

Prints de referência e specs de layout de cada tela — nenhum ainda nesta
estrutura nova. O layout mais detalhado que existe hoje é o da tela de
venda WinForms (`legacy/GestorPDV.Vendas/VendaForm.cs` + o print de
referência que guiou aquele layout); a VendaView em WPF
(`src/GestorPDV.App/Views/Vendas/VendaView.xaml`) ainda não foi
redesenhada pixel a pixel a partir dele — é funcional, não uma cópia
visual.
